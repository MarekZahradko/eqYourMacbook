# macOS
.DS_Store

# Xcode project — not used by the build (see scripts/build.sh), kept out of
# git in case anyone generates one locally for IDE browsing
*.xcodeproj/
*.xcworkspace/
xcuserdata/
*.xcuserstate

# Build artifacts
build/
DerivedData/

# Swift Package Manager
.build/
.swiftpm/
