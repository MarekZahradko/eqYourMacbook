# Single source of truth for build settings. Sourced by the repo-root entry-point
# scripts (build.sh / install.sh / release.sh / run.sh).
# Keep in sync with Package.swift and Resources/Info.plist.

APP_NAME="eqYourMacbook"
BUNDLE_ID="com.zdenekkops.eqyourmacbook"
VERSION="1.0.1"
DEPLOYMENT_TARGET="14.4"
SWIFT_VERSION="6"
DEFAULT_ISOLATION="nonisolated"
UPCOMING_FEATURES="MemberImportVisibility"
